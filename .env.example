# Copy to .env and adjust as needed.  All values here match the project
# defaults, so you can delete .env and everything still works.
OLLAMA_HOST=http://192.168.68.59:11435
EMBEDDING_MODEL=nomic-embed-text:latest
CHAT_MODEL=gemma3:4b
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
EMBEDDING_BATCH_SIZE=10
MAX_CONTEXT_CHARS=12000
# Optionally point the app at a different documents folder / vector DB:
# DOCUMENTS_PATH=documents
# CHROMA_PATH=chroma_db
